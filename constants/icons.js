import { baseUrl } from '../base/base'

const icons = {
  facebook: `${baseUrl}/images/facebook-icon.png`,
  linkedin: `${baseUrl}/images/linkedin-icon.png`,
  instagram: `${baseUrl}/images/instagram-icon.png`,
}

export default icons
